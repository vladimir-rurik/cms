apiVersion: apps/v1
kind: Deployment
metadata:
  name: page-service
  namespace: universal-cms
  labels:
    app: universal-cms
    component: page-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: universal-cms
      component: page-service
  template:
    metadata:
      labels:
        app: universal-cms
        component: page-service
    spec:
      containers:
      - name: page-service
        image: universal-cms/page-service:latest
        ports:
        - containerPort: 3002
        env:
        - name: PORT
          value: "3002"
        - name: COMPONENT_SERVICE_URL
          value: "http://component-service:3001"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: page-service
  namespace: universal-cms
  labels:
    app: universal-cms
    component: page-service
spec:
  selector:
    app: universal-cms
    component: page-service
  ports:
  - name: http
    port: 80
    targetPort: 3002
  type: ClusterIP